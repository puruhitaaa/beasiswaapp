{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@beasiswaapp/auth": "workspace:*",
    "@beasiswaapp/db": "workspace:*",
    "@beasiswaapp/env": "workspace:*",
    "@fastify/cors": "^11.3.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "fastify": "^5.10.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@beasiswaapp/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.22.14",
    "tsx": "^4.23.1",
    "typescript": "catalog:"
  }
}
